import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Login — CRM IntegraSistemas",
}

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
